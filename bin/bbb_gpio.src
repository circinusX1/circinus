// SCRIPT  NOT TESTED
::using(eGPIO|ePWMM|eSRV);

setdbg(0xFF);

sys_config(ePWMM, ["/dev/pwm/","/*path fmt*/","/*chip fmt*/"]);                    // use /dev/pwm/PWM_PIN

var pwm2 = PWM("ecap0", 30000, 50, false, "pwm");

function main(ctx)
{
   return run(l,10);
}

var x=0;
function l(c,d)
{
    pwm.set_value(x++);
    if(x>99)
        x=0;
    return true;
}

