Debug(3);
function main(xxx)
{
    local ser = Device("ssh://s2w/s2w@192.168.1.200:22","remote",TEXT);
    ser.write([13]); /*CR*/
    ser.strexpect("s2w@s2w:")

    return Run();
}


function loop(dev, type, data)
{
    if(type==TEXT)
    {
        print("received:" + data);
        print("\n");
    }
    return 0;
}
